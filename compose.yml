version: "3.8"
services:
  analytics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: analytics
    environment:
      MODE: ${MODE:-jupyterlab}
    ports:
      - 8888:8888 # jupyterlab
    volumes:
      - ./:/app